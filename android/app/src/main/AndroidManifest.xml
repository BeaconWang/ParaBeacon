<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bluetooth Low Energy (external flight sensors). Android 12+ uses the
         BLUETOOTH_SCAN / BLUETOOTH_CONNECT runtime permissions; older versions
         fall back to location. usesPermissionFlags="neverForLocation" declares
         we don't derive physical location from BLE scans. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Legacy permissions for Android 11 and below. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Fine + coarse location: used by legacy BLE scanning (Android 11-) and
         by the Map control's device GPS (geolocator) across all SDK levels. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location (Android 10+): lets the flight recorder keep
         receiving GPS fixes once the app is backgrounded / the screen is off.
         Granted separately by the user ("Allow all the time"); the app still
         works with only foreground ("while in use") permission, but the track
         may pause when fully backgrounded without this. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Required to download online map tiles for the Map control. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Background execution: keep GPS/vario/BLE working with the screen off or
         the app backgrounded. WAKE_LOCK keeps the CPU alive (also used by the
         keep-awake wakelock). When a flight starts the geolocator stream is
         (re)opened with a foreground-service configuration, which starts the
         plugin's GeolocatorLocationService (declared with
         foregroundServiceType="location" in the geolocator_android manifest and
         merged in here) so location + recording survive the screen turning off.
         FOREGROUND_SERVICE / FOREGROUND_SERVICE_LOCATION authorize that service
         on Android 10+/14+, and POST_NOTIFICATIONS (Android 13+) is required to
         show its persistent notification. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="ParaBeacon"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
